---
interface Props {
  extraCss?: string | undefined;
}

const { extraCss } = Astro.props;

import pattern from "@assets/pattern.svg";
---

<div class:list={[extraCss]} style=`background-image: url(${pattern.src})`>
  <slot />
</div>
